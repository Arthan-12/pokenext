import React from 'react';

const PokemonGlobalContext: React.FC = ({ children }: any) => {
  return (
    <>{/* <PokemonContextProvider>{children}</PokemonContextProvider>; */}</>
  );
};

export default PokemonGlobalContext;
